name: dl

channels:
  - pytorch
  - conda-forge
  - defaults

dependencies:
  - matplotlib

  - pytorch-lightning
  - torchmetrics
  - wandb

  - pip
  - pip:
      - lightning-bolts
      - kaggle
      - timm
      - einops
      - submitit